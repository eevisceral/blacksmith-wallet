# Optional overrides for local fork / deploy quotes.
# Copy to .env and fill in. Never commit .env.
# Deploy quotes read ETH_RPC / BASE_RPC (CHAINS snapshot if unset).

# ETH_RPC=https://gateway.tenderly.co/public/mainnet
# BASE_RPC=https://base.rpc.sentio.xyz
